# AlgoJudge Server

**The documentation is one level up**, in the repository's own
[README](../README.md): what this Server is and is not, how to build, test and
run it, where the files go, what encrypts a session cookie, how to configure an
installation from disk, and how to register an identity provider.

Nothing is documented here on purpose. Until 2026-08-30 this file was the
heading alone, which gave a reader who opened the project directory first no
content and no pointer; a second copy of the content would be a second thing to
keep true.
